extends ../layout/index

block contenido
    div.py10
        h1.text-4xl.my-10.font-extrabold.text-center Bienes
            spam.font-normal Raices
        h2.text-center.txt2xl.font.extrabold= pagina
    div(class= "bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class= error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text.white.uppercase= mensaje

        if !error
            a(href="/auth/login" class="text-center text-sm text-slate-600 uppercase mt-10 block") Ya puedes Iniciar Sesión